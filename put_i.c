#include "main.h"
/**
 * put_i - function to write an int to the stdout
 * @num: number to be written
 *
 * Return: number of times written
 */

int put_i(int num)
{
	int count = put_d(num);

	return (count);
}
